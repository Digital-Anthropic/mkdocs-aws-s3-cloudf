locals {
    terraform_s3_cloudfront_lambda_func = {
    "mkdocs-bucket" = {
      bucket_name               = "mkdocs-bucket"

    }
    
    "modules-bucket" = {
      bucket_name               = "modules-bucket"

    }
}
}